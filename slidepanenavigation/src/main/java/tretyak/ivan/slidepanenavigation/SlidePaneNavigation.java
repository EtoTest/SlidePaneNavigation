package tretyak.ivan.slidepanenavigation;

import android.content.Context;
import android.support.v4.widget.SlidingPaneLayout;
import android.util.AttributeSet;

public class SlidePaneNavigation extends SlidingPaneLayout {
    public SlidePaneNavigation(Context context) {
        super(context);
        init();
    }

    public SlidePaneNavigation(Context context, AttributeSet attrs) {
        super(context, attrs);
        init();
    }

    public SlidePaneNavigation(Context context, AttributeSet attrs, int defStyle) {
        super(context, attrs, defStyle);
        init();
    }

    private void init(){

    }
}
